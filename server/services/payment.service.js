const Order = require("../models/Order.Model");

async function initiatePayment(order, userId, stripeSession) {
  try {
    
  } catch (err) {
    throw new Error(err);
  }
}


module.exports = {
    initiatePayment,
}